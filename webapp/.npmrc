; Fail on unmet peer dependencies rather than shipping a broken tree.
strict-peer-dependencies=true

; Keep the node_modules tree strict: a package must declare what it imports.
shamefully-hoist=false
auto-install-peers=true

; Reproducible installs in CI and in the Docker build.
resolution-mode=highest
